# Azure Developer CLI Configuration
# NOTE: The recommended deployment method is deploy_to_azure.ps1
# This file is provided for Azure Developer CLI (azd) compatibility
name: zava-logistics
metadata:
  template: python-web@1.0.0

services:
  web:
    project: .
    language: python
    host: appservice

infra:
  provider: bicep
  path: infra

pipeline:
  provider: github
